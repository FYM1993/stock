# v16改进验证

## 🔧 v16改进内容

### 改进1：调整评分权重
```python
涨幅：    50分 → 60分（最重要）
量能：    20分 → 30分（资金集中）
提前启动：30分 → 10分（参考）
```

### 改进2：优化量能计算
```python
# 旧：前半 vs 后半
vol_ratio = 后4天平均 / 前4天平均

# 新：当前 vs 历史
vol_ratio = 当前8天平均 / 历史30天平均
```

---

## 📊 预期改进效果

### 新易盛评分对比
```
v15:
- 涨幅21.4% → 30分（50分制）
- 提前13% → 20分（30分制）
- 量比0.9 → 0分（前半vs后半）
- 总分：50分

v16预期:
- 涨幅21.4% → 40分（60分制）
- 量比2.8倍 → 20分（vs历史）
- 提前13% → 7分（10分制）
- 总分：67分 ↑17分
```

### 候选池排名
```
v15: 排名8+，未进Top10
v16: 预期Top5，进入候选池
```

### 最终收益
```
v15: +19.4%（买了大为科技等）
v16: 预期+30-35%（能买到新易盛）
```

---

## ✅ 验证检查点

### 1. 真龙头评分是否提高？
```bash
grep "真龙头诊断-v16.*300502" logs/backtest_v16.log | grep "2025-06"
```

**目标**：
- 总分 > 60分（v15是50分）
- 量能分数 > 10分（v15是0分）

### 2. 是否进入候选池Top10？
```bash
grep "候选股票:.*300502" logs/backtest_v16.log
```

**目标**：
- 新易盛出现在候选股票列表中

### 3. 是否成功买入？
```bash
grep "方向=1" logs/backtest_v16.log | grep "300502"
```

**目标**：
- 有买入300502的订单

### 4. 最终收益如何？
```bash
tail -100 logs/backtest_v16.log | grep "总值="
```

**目标**：
- 总值 > 130,000（v15是119,416）
- 收益率 > 25%（v15是19.4%）

---

## 📝 监控命令

```bash
# 查看当前进度
tail -20 /Users/yimin.fu/.cursor/projects/Users-yimin-fu-GolandProjects-stock/terminals/58867.txt

# 查看真龙头评分
grep "🎯.*v16.*300502\|🎯.*v16.*300308\|🎯.*v16.*601869" logs/backtest_v16.log

# 查看候选池
grep "候选股票:" logs/backtest_v16.log | grep "2025-06"

# 查看买入记录
grep "方向=1" logs/backtest_v16.log | grep "2025-0[56]"
```

---

## 等待回测完成...

预计时间：30-40分钟
